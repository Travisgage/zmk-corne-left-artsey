name: Build ZMK firmware

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout config repo
        uses: actions/checkout@v4

      - name: Initialize ZMK
        uses: zmkfirmware/zmk-build-action@v1.1.1
        with:
          config: .